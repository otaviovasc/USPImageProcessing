<%= simple_form_for(@image) do |f| %>
  <div class="form-inputs">
    <%= f.input :original_image, as: :file %>
  </div>
  <div class="form-actions">
    <%= f.button :submit, "Processar foto"%>
  </div>
<% end %>
